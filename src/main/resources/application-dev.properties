# Ativo somente quando perfil "dev" estiver ligado
# Baseline automático: adota o estado atual do banco e cria flyway_schema_history
spring.flyway.baseline-on-migrate=true

# (Opcional) logs um pouco mais verbosos para investigar
logging.level.org.flywaydb=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

